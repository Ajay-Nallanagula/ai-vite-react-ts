name: 🔍 Code Review Agent

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  pull-requests: write
  contents: read

jobs:
  code-review:
    runs-on: ubuntu-latest
    name: Analyze PR Code

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: 📚 Install dependencies
        run: npm ci

      - name: 🔍 Run code review analysis
        id: review
        run: |
          npx ts-node scripts/codereview.agent.ts \
            --owner=${{ github.repository_owner }} \
            --repo=${{ github.event.repository.name }} \
            --pr=${{ github.event.pull_request.number }}

      - name: 📤 Post review comment
        if: always()
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const path = require('path');
            
            const reviewFile = path.join(
              process.cwd(),
              '.github',
              'review-data',
              `review-pr-${{ github.event.pull_request.number }}.json`
            );
            
            if (fs.existsSync(reviewFile)) {
              const reviewData = JSON.parse(fs.readFileSync(reviewFile, 'utf-8'));
              
              github.rest.issues.createComment({
                issue_number: context.issue.number,
                owner: context.repo.owner,
                repo: context.repo.repo,
                body: reviewData.comment
              });
              
              console.log('✅ Review comment posted to GitHub');
            } else {
              console.log('ℹ️ No issues found in code review');
            }

      - name: 📊 Generate summary
        if: always()
        run: |
          echo "## 📊 Code Review Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- PR Number: ${{ github.event.pull_request.number }}" >> $GITHUB_STEP_SUMMARY
          echo "- Author: ${{ github.event.pull_request.user.login }}" >> $GITHUB_STEP_SUMMARY
          echo "- Review Status: ✅ Complete" >> $GITHUB_STEP_SUMMARY
